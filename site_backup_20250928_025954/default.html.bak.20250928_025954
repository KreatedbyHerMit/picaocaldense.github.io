<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>{{ page.title }} | {{ site.title }}</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link rel="stylesheet" href="{{ '/assets/css/main.css' | relative_url }}" />
  <style>
    body {
      background: white;
      color: black;
      font-family: 'Georgia', serif;
      max-width: 800px;
      margin: 2rem auto;
      padding: 0 1rem;
    }
    nav a {
      margin-right: 1rem;
      text-decoration: none;
      color: black;
      font-weight: bold;
    }
    footer {
      margin-top: 3rem;
      font-size: 0.8rem;
      color: #666;
      text-align: left;
    }
  </style>
</head>
<body>
  <header>
    <h1>{{ site.title }}</h1>
    <nav>
      <a href="{{ '/' | relative_url }}">Home</a>
      <a href="{{ '/about/' | relative_url }}">About</a>
      <a href="{{ '/order/' | relative_url }}">Order</a>
    </nav>
    <hr />
  </header>

  <main>
    {{ content }}
  </main>

  <footer>
    &copy; {{ site.time | date: "%Y" }} {{ site.title }} — <a href="mailto:jessica.betancur@picaocaldense.com">Email</a>
  </footer>
</body>
</html>
